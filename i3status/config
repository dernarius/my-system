general {
	interval = 1
	colors = true
	separator = " "
}

order += "battery 0"
order += "tztime local"

battery 0 {
	format = "%percentage (%emptytime)"
}

tztime local {
  format = "%Y-%m-%d %H:%M:%S"
}
